RCONV progress marker: write access verified.

Status: STOP-AND-REPORT before assembly edits. No GPU dispatch or executable kernel/HIP program was run.

Source-location verification (live occ_kernel_dsws_flow.s): role count slots are at 381-383; CONVCNT_OFF is at 480; CFASSIGN guards are at 971-984; ROLE initialization is at 3030-3041; ROLE[wid] is read at 3292-3314; feed dispatch is at 3924-4037; CFASSIGN cohort assignment is at 4074-4115; coast begins at 4870. The cited locations exist, subject to normal line drift.

Piece 1: no change landed. The physical mailboxes and coast insertion point were verified. No threshold/cooldown state was added because Piece 3 requires a stop-and-report first.

Piece 2: no change landed. Important source finding: NCOMP_SLOT, NAFEED_SLOT, and NBFEED_SLOT have definitions at 381-383 and are read by trace_row at 2850-2855, but the live coordinator initialization from 3010-3085 does not write any of those three slots. A floor-guarded conversion decrement cannot be honest until the specified initial census values and their ownership are established.

Piece 3 eligibility-contract trace: at 4085-4103 the current cohort is a static WAVES-wide index partition. At 4104-4105 s15 is compared only to the absolute cohort end, and at 4106-4111 eligibility is exactly wid (s24) < cohort_width, yielding r = cohort_start + wid. The no-unit path at 4112-4114 also stamps the same end token. No ROLE mailbox read, role-count-slot read, or role-dependent condition occurs in that path. The sole ROLE[wid] read is earlier at 3292-3314. Both ROLE_COMPUTE and feed roles can reach .Lflow_feed_empty: compute reaches .Lflow_coast at 3317-3323 and .Lflow_coast can branch to .Lflow_feed_empty at 4870-4876; feed reaches it directly at 3924-3929. Therefore conversion neither includes nor excludes a wid from the source-visible CFASSIGN cohort. s15 requires no reset under this static-wid contract, but the spec requires a membership-aware re-key and supplies no rule defining membership by role or a replacement unit mapping. The minimal correct re-key is consequently not derivable without inventing a new semantic contract. STOPPED rather than guessing.

Baseline byte-identity check: existing occ_dsws2_w30_flow_gd.bin SHA-256 = cac3ff7c2338e73f8fe47c115b592ddcd5afa1014ea77c6da66a37eea4fd3553; required prefix/value cac3ff7c2338e73f is present. This was read after the progress-file update; no assembly or build-script edit occurred.

Offline gates at stop: DSWS2_RCONV=0 baseline hash green as above. DSWS2_RCONV=1 RGA spill check not run because no DSWS2_RCONV implementation was written. Both requested new .error-guard checks not run for the same reason. CPU control model untouched and not run.

Worktree note: the final spike-local diff inspection showed a pre-existing unstaged hunk in occ_kernel_dsws_flow.s at 980-982 (the CFASSIGN && DSWS2_CONV guard). It was not authored, edited, staged, reverted, or otherwise touched during this task. The only file written by this task is this progress file.

2026-07-23 Piece 1 resumed under HANDOFF_RCONV_PIECE1_2026-07-23.md. The live source was rechecked before editing: `ROLE_BASE` and role values are at occ_kernel_dsws_flow.s:624-629, role initialization at :3025-3041, the mailbox read/adopt loop at :3292-3314, the compute-success path at :3324, and coast entry at :4870. The established pre-existing unstaged source hunk remains only :980-982 and is not mine.

Coast-counter SGPR selection: s50. `rg` finds exactly its initialization at :2998 and no read in the whole source; it is therefore dead across the coast -> feed/compute -> loop round trip. Although its existing comment calls it a coordinator period counter, the coordinator-period path at :3273 has no s50 use. Conversion can only begin from ROLE_COMPUTE, whose initial waves are wid >= 3, so this private counter cannot affect wid0 coordinator state. s69 was rejected: it is read by the decentralized boundary path at :4323 (and coordinator claim at :3176), so it is live.

Selected threshold: DSWS2_RCONV_COAST_N=64. The initial role split is three feeds (wid0 B, wid1 A, wid2 B) and 27 computes; unidirectional conversion has no floor gate, so the theoretical worst case after every compute converts is one A feed plus 29 B/A feeds and zero computes. This handoff permits that first-build risk and relies on the threshold/self-extinguishing ring fill; no census changes are made. The mailbox seam was rechecked: :3295 loads ROLE[wid], :3298 detects the change, and :3305 only performs the existing defensive shrink-to-lean (there is no grow at role adoption), then :3312 dispatches non-compute roles to feed.

Piece 1 assembly landed in occ_kernel_dsws_flow.s only. Definitions are at :402-407 (`DSWS2_RCONV=0`, `DSWS2_RCONV_COAST_N=64`); guards at :986-996; the successful-compute reset at :3339-3342; and the threshold-only note-drop at :4888-4908. On equality with N, the branch calculates `ROLE_BASE + wid*4`, uses `lds_put_r` to write `ROLE_AFEED`, then lane 0 increments `occ[48]` at `CONVCNT_OFF` and drains STORE_CNT. The hot coast path has no new traffic before the threshold branch and contains no `s_alloc_vgpr`, barrier, or message-bus operation. The conversion write is consumed on the following loop pass by the existing `lds_get_r s35, ROLE[wid]` at :3295; the only resize is the pre-existing lean `s_alloc_vgpr 32` at :3305-3307, never a grow.

Latest offline checks (2026-07-23, all CPU-only): the exact A1 `DSWS2_RCONV=0 DSWS2_CONV=0 CFASSIGN=1` rebuild produced SHA-256 `cac3ff7c2338e73f8fe47c115b592ddcd5afa1014ea77c6da66a37eea4fd3553`, and `cmp` with the canonical `occ_dsws2_w30_flow_gd.bin` returned 0. The A1 `DSWS2_RCONV=1 DSWS2_CONV=0 CFASSIGN=1 RGADESC=1` assembly and link both succeeded. Guard checks were freshly run: `DSWS2_RCONV=1 CFASSIGN=0 DSWS2_CONV=0` failed at :993 with `DSWS2_RCONV requires CFASSIGN=1.`; `DSWS2_RCONV=1 CFASSIGN=1 DSWS2_CONV=1` failed at :987 with `DSWS2_RCONV and DSWS2_CONV are mutually exclusive.`

STOP-AND-REPORT verification blocker: RGA produced no stats or spill count. A fresh direct RGA run on the successfully assembled/linkable RCONV=1 code object exited 0 but emitted only `Failed opening file /home/kmbandy/.rga/GPUOpen/rga/rga_cli41-20260723-084737.log for writing: Read-only file system`; no ISA, stats, or livereg files were created. The same mandatory-log failure also occurs for `rga --help`, and the visible CLI help exposes no supported alternative log directory. Therefore 0-spill is NOT claimed and the four-gate completion condition is not green. No GPU dispatch, binary execution, HIP/ROCm program, staging, or commit was performed.
