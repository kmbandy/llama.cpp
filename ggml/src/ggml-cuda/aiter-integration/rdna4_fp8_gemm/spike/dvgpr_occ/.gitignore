# Build artifacts (reproducible via ./build.sh)
*.o
*.bin
test_oracle
occ_dispatch

# wmma_seed.hip --save-temps intermediates (one-time encoding probe, Task 2)
wmma_seed-*
*.hipfb
*.hipi
*.bc

# Approach-2 cross-check (P3-T5): binary + hipcc --save-temps intermediates
gemm_occ_pad
gemm_occ_pad-*
gemm_occ_pad_*
rtc_seed.hip
rtc_seed-*
