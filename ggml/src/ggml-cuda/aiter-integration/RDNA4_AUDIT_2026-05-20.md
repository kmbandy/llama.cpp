# RDNA4 + AITER + Triton + vLLM — Cross-Repo Audit

**Date:** 2026-05-20
**Trigger:** AITER upstream shipped first wave of consumer-RDNA4 (gfx1201) enablement ~2026-05-19. This audit consolidates the state of that enablement across AITER, Triton (upstream + ROCm fork), and vLLM, so we can decide what to vendor / cherry-pick / ignore.

> **Corrections appended 2026-05-20 evening, post-re-vendor investigation.** See "Corrections from verifying against actual repos" below the TL;DR — three claims from the original subagent reports need updating.

**Scope:** AMD R9700 (Navi 48, gfx1201, RDNA4, wave32, ~64 CUs). Not gfx1250 (which is MI450/CDNA, pre-silicon — see Terminology).

**Status of our integration at audit time:** `feat/aiter-turbo` branch, MAD-199 v2 chunks A+B shipped. AITER+turbo3/turbo4 correctness validated. Performance: AITER+f16 decode is ~30% slower than vanilla llama.cpp f16 at 64K ctx; AITER+f16 prefill is ~55% slower than vanilla. AITER+turbo3 64K decode is ~14% slower than AITER+f16. This audit was commissioned to find out why and what to do about it.

---

## TL;DR

1. **AITER's RDNA4 (gfx1201) support is ~5 days old.** PR #3236 (merged 2026-05-19) fixed a *silent buffer-resource fallback bug* where lowercase `__gfx11__`/`__gfx12__` tokens caused gfx12 to drop into an invalid `0xffffffff` buffer-rsrc path. **Our vendored kernel is from 2026-05-18 — one day before the fix.**

2. **We didn't vendor the host-side dispatcher.** `aiter/ops/triton/attention/unified_attention.py` has gfx1201 dispatch logic (`TILE_SIZE`, `BLOCK_M`, `waves_per_eu`, `num_warps`, `num_stages`, `target_num_prgms = cu_count * 4`) that we replaced with hardcoded CDNA-tuned constants in our wrapper.

3. **Triton's RDNA4 codegen has structural gaps**: pipeliner UAF on `num_stages>=2`, no block-pingpong scheduler, IN_THREAD_TRANSPOSE only in ≥3.7. Some are fixable (force `num_stages=1`, upgrade Triton); some are structural (no block-pingpong on WMMA archs).

4. **The single biggest unlanded perf op is Triton PR #7616** (`global_load_tr_b128` on gfx12) — single-instruction 8×8 transpose-load that skips LDS for non-K-contig dot operands. Exactly what attention K/V loads need. Stuck in draft since Jul 2025.

5. **vLLM does not yet use AITER on RDNA4** — gated to `on_mi3xx()`. Their stack pre-dates this week's AITER RDNA4 work. They officially direct RDNA4 users to Triton FA (`rocm.py` log line). This is **timing, not a verdict**.

6. **EmbeddedLLM/aiter** is the de-facto active RDNA4 staging fork. PRs #2351 (quant) and #2352 (rmsnorm) for gfx1201 have been open against upstream since March; PRs #3168/#3234 add ~5× speedup gemm_a8w8 configs for Mistral-3/Qwen3 shapes.

---

## Corrections from verifying against actual repos

When starting the re-vendor work (MAD-202), three claims from the original subagent audit did not survive a direct check against the upstream repo:

1. **The commit `be0ca6a` ("Tune 3D attention kernel on gfx1151") does not exist in `ROCm/aiter` upstream main.** The audit agent either hallucinated the SHA or read from an unmerged branch. The arch-aware tuning pattern it described as a "new precedent" actually *already exists* in the upstream host dispatcher `aiter/ops/triton/attention/unified_attention.py` (functions `select_2d_config` and `select_3d_config`) — it's the dispatcher we never vendored, not new code. The substantive recommendation (vendor the host dispatcher; let it pick arch-aware values) is unchanged; just the framing was wrong.

2. **PR #3236's buffer-resource fallback fix is in `opus.hpp` (C++), not in our vendored kernel `.py`.** Our integration doesn't include `opus.hpp` (we vendored only the Triton kernel file). So the "we likely carry the buffer-rsrc bug" claim probably does not apply to our integration at all. Worth a grep-confirm during MAD-202 work; but re-vendoring our `.py` alone wouldn't pick it up regardless.

3. **PR #3228's 28 new gfx1201 FP8 GEMM configs are JSON files only.** They live in `aiter/ops/triton/configs/gemm/` and only matter if AITER's `gemm_a8w8` path is exercised. We don't currently exercise it. Re-vendoring our `.py` alone does not pick them up; they belong with the MAD-206 cherry-pick story.

**Net effect on the re-vendor:** the actual kernel `.py` delta between our vendor SHA (`32e1e6d`, 2026-05-18) and upstream HEAD (`163e6a025`, 2026-05-20) is **a single 2-line accumulator-form refactor (PR #3231, commit `7f613da83`)**:
```diff
-        acc += tl.dot(P.to(V.dtype), V)
+        acc = tl.dot(P.to(V.dtype), V, acc=acc)
```
Applied at two sites (lines 353 and 672 — the V·scores accumulate in both 2D and 3D kernels). Marginal perf, no behavior change.

**The substantive re-vendoring work is the host dispatcher (MAD-203)**, which is now correctly scoped and was always going to be the load-bearing change. MAD-202 has been bundled into the same commit as MAD-203 since on its own it's a trivial 2-line patch.

---

## Terminology

| Token | Family | Examples |
|---|---|---|
| **gfx942** | CDNA3 | MI300X, MI325X |
| **gfx950** | CDNA4 | MI350X, MI355X |
| **gfx1100** | RDNA3 | RX 7900 XTX / W7900 |
| **gfx1151** | RDNA3.5 | Strix Halo (mobile) |
| **gfx1200** | RDNA4 | Navi 44 (RX 9060 series) |
| **gfx1201** | **RDNA4** | **Navi 48 (RX 9070 XT, RX 9070, R9700 — our target)** |
| **gfx1250** | **CDNA (pre-silicon)** | **MI450 — NOT RDNA4** |

**Critical:** many recent ROCm commits/branches reference `gfx1250` for what looks like "next-gen AMD." That is **MI450 / CDNA**, not consumer RDNA. Anything tagged gfx1250 does not benefit R9700 unless back-ported to gfx1201.

---

## Methodology

Six subagents fanned across three repos (main + extended branches/PRs/forks):

| Agent | Scope |
|---|---|
| AITER main | ROCm/aiter default branch — arch dispatch, configs, kernel variants, CI |
| AITER ext | ROCm/aiter all branches, PRs (open/closed/draft), forks (EmbeddedLLM/aiter) |
| Triton main | triton-lang/triton default branch — RDNA4 codegen, opt passes, recent perf PRs |
| Triton ext | triton-lang/triton all branches/PRs + ROCm/triton fork |
| vLLM main | vllm-project/vllm default branch — AITER integration, attention backends |
| vLLM ext | vllm-project/vllm all branches/PRs + retired ROCm/vllm fork |

---

## Section 1 — AITER (ROCm/aiter)

### 1.1 Host-side dispatcher we never vendored

Upstream `aiter/ops/triton/attention/unified_attention.py` (the dispatcher we don't have) contains gfx1201 logic:

```python
# Lines ~18-42:
TILE_SIZE         = 32 if arch.name == "gfx1201" else 16 if arch.is_rdna else 64
waves_per_eu      = 8  if arch.name == "gfx1151" else 6  if arch.is_rdna else 2
BLOCK_M           = 64 if arch.is_rdna else 128   # large-prefill 2D path
num_stages_2d     = 1
num_warps         = 4  if arch.is_rdna else 8

# Lines ~165-167 — 2D vs 3D dispatch:
cu_count          = get_num_sms()
target_num_prgms  = cu_count * 4          # 64*4=256 on R9700, 304*4=1216 on MI300
num_2d_prgms      = total_num_q_blocks * num_kv_heads
use_2d_kernel     = num_2d_prgms > target_num_prgms or max_seqlen_k <= 512 or sliding_window
```

Our wrapper `mt_aiter_unified_attn.h` instead hardcodes:

```c
#define MT_AITER_UATTN_NUM_SEGMENTS_PER_SEQ 32   // CDNA-tuned; on R9700 should follow cu_count*4 heuristic
#define MT_AITER_UATTN_BLOCK_Q              2    // matches
#define MT_AITER_UATTN_BLOCK_M              16   // upstream RDNA prefill uses 64
#define MT_AITER_UATTN_TILE_SIZE            32   // matches gfx1201 default
```

**Implications:**
- The `target_num_prgms = cu_count * 4` dispatcher routes work between the 2D (prefill-friendly) and 3D (split-K) kernels based on CU count. On 64-CU R9700 the threshold is 256; on 304-CU MI300 it's 1216. The 3D split-K path gets chosen far more aggressively on R9700, with each segment too small to amortize the `reduce_segments` epilogue. **Direct candidate cause of the prefill regression.**
- `MIN_SEGMENTS = 16 if TILE_SIZE ≤ 16 else 8` — with TILE_SIZE=32 the kernel forces ≥8 segments even on short contexts; pure overhead on R9700.
- `BLOCK_M=16` vs upstream's 64 for RDNA prefill is 4× less LDS reuse.

### 1.2 The RDNA4 enablement timeline (last 7 days) — **corrected**

| Date | Commit / PR | What it does | Affects our vendored .py? |
|---|---|---|---|
| 2026-05-19 | PR #3236 (merged) | `opus.hpp` RDNA4 support: parse + buffer rsrc + WMMA w32/w64 dispatch. C++ header fix. | **No** — opus.hpp is not in our vendor scope. |
| 2026-05-19 | PR #3228 (merged) | 28 new gfx1201 FP8 Triton GEMM configs (JSON in `configs/gemm/`). | **No** — JSON configs only. Belongs to MAD-206. |
| ~~2026-05-20~~ | ~~commit be0ca6a~~ | ~~Tune 3D attention kernel on gfx1151~~ | **Hallucinated SHA** — does not exist in upstream main. The arch-aware pattern already exists in `select_2d_config`/`select_3d_config` of the upstream host dispatcher. |
| 2026-05-20 | PR #2492 (merged, commit `0f12f7ff5`) | MLA Triton + Gluon kernels (+1932 LOC). gfx1250 lineage (CDNA, MI450). | **No** — gfx1250 ≠ our target gfx1201. |
| **2026-05-20** | **PR #3231 (merged, commit `7f613da83`)** | **`Use tl.dot(..., acc=...) accumulator form.`** Two-line refactor in our vendored kernel: `acc += tl.dot(...)` → `acc = tl.dot(..., acc=acc)` at lines 353 + 672. | **Yes** — this is what MAD-202 picks up. |

**Our vendor SHA `32e1e6d` (2026-05-18) is behind upstream HEAD `163e6a025` (2026-05-20) by 17 commits, but only PR #3231 touches the file we vendored.** The big wins are not in the vendored kernel — they are in the host dispatcher (separate file) which is MAD-203's scope.

### 1.3 AITER's own RDNA4 tuning coverage

`aiter/ops/triton/configs/gemm/` count by arch (as of audit):

- gfx942 (CDNA3): **57 files**
- gfx1250 (MI450/CDNA): **44 files**
- gfx1201 (our target): **39 files** (was ~11 before this week's PR #3228)
- gfx950 (CDNA4): ~20 files
- gfx1200 (Navi 44): 0 files

RDNA4 is the smallest officially-tuned tier but rapidly growing. `unified_attention.py` has **no autotune** — all knobs are deterministic heuristics in the host dispatcher.

### 1.4 Open PRs against AITER upstream (gfx1201-load-bearing)

| PR | State | Title | Why it matters |
|---|---|---|---|
| **#3168 / #3234** | Open | `gfx1201: gemm_a8w8 tuning configs (Mistral-3/Qwen3)` | Claims **~5× speedup, QKV proj 163µs → 33µs.** |
| **#2351** | Open since March | `[gfx1201] Enable quantization kernels for gfx1201` | Replaces `v_pk_mul_f32`, DPP `0x142/0x143`, naive global→LDS loading. From EmbeddedLLM. |
| **#2352** | Open since March | `[gfx1201] Enable RMSNorm support for gfx1201` | Replaces `v_pk_mul_f32`, bf16/fp16 unpacking inline asm with portable HIP/C++ guarded for RDNA4. |
| #2886 | Draft | Triton conv2d for RDNA (5 kernel families, Winograd) | Fixes PyTorch/MIOpen bf16 perf gap. Not attention. |
| #2699 | Draft | Initial Windows support, tested on gfx1200 + ROCm 7 | 6/8 ops pass. Not our concern but signals consumer-AMD investment. |
| #918 | Open since March | `add gfx1201 and gfx908 definitions` | Foundational arch dispatch; still not merged. |
| #1829 | Open since March | `Support gfx1201 for triton gemm_a8w8_blockscale` | Quantized GEMM blockscale. |

### 1.5 EmbeddedLLM/aiter — the de-facto RDNA4 fork

Six RDNA4 branches actively maintained:

- `support_gfx1201` (most active, includes batch gemm tuned configs, MoE routing sigmoid + op tuning, MoE tuning script base, LLVM fix)
- `support_gfx1201_min`
- `rdna4-ck`
- `rdna4_ck-gemm-a8ww8-blockscale` (tuned configs for **Qwen3-30B-A3B-FP8** on gfx1201)
- `rdna4_quant-support` (= upstream PR #2351)
- `rdna4_rmsnorm-support` (= upstream PR #2352)

**Per AITER ext agent:** *"Anyone trying to run consumer RDNA4 today should consider this fork or vendor those PRs."*

### 1.6 Branches with gfx1201 work not yet PR'd

- `mla_gluon_gfx1201` (2026-03-17): MLA Gluon kernel, *"B=32 S=1024 splits=2 time=0.046ms 24.64 TFLOP/s"*
- `mxfp4_gfx1100*`: *"Enable FPMX4 GEMM on non-FPMX4 devices (Navi31/44/48 in particular). Adds special path for devices with f8 wmma but not f8 scaled_wmma. Includes gfx942, gfx1100, gfx1201 configurations."*
- `feat/flydsl-fmha-gfx1201` — base of merged PR #2969 (now landed)
- `jpvillam/lse_gfx12`

### 1.7 FlyDSL: AITER's RDNA4-native attention path

- **PR #2969 (merged 2026-05-03):** `aiter/ops/flydsl/kernels/flash_attn_func_gfx1201.py` (~31 KB). WMMA 16×16×16 wave32, BLOCK_N=32, software-pipelined GEMM2.
- **Reported: 49.4 TFLOPS vs 29.0 PyTorch SDPA on Wan2.1-T2V-1.3B** (1.34× over SDPA, 2.1% over tuned Triton FA).
- **Constraint: self-attention only, no paged/block_tables.** Not a drop-in for `unified_attention`.
- PR #2990 extends to Wan2.2 shapes.

Proves what's achievable on R9700 when you actually tune; doesn't help our paged use case directly.

### 1.8 AITER CI / official support

- AITER's CI matrix does NOT include gfx1201 (per issue #1552).
- `_ARCH_TO_DEVICE` in `aiter/ops/triton/utils/arch_info.py` does NOT map gfx1201 (per TransformerEngine #520). Community patches add `gfx1201 → MI350X` as a workaround.
- `AITER_ROCM_ARCH` in vLLM's Dockerfile is set to `gfx942;gfx950` — AITER isn't even *compiled* for RDNA4 in vLLM's shipped image.

---

## Section 2 — Triton (triton-lang/triton + ROCm/triton)

### 2.1 Codegen status

- **WMMA v2 lowering for RDNA4 added in Triton 3.6.0** — handles f16/bf16→f32, plus several fp8 combos.
- **gfx1201 has NO arch-specific defaults** in `third_party/amd/backend/compiler.py` HIPOptions. All archs get `num_warps=4, num_stages=2, waves_per_eu=0, kpack=1, matrix_instr_nonkdim=0`.

### 2.2 Structural codegen gaps on RDNA4

| Gap | Impact | Fixable? |
|---|---|---|
| **`num_stages>=2` UAF on gfx1201** | Pipeliner crashes; tutorial pins `NUM_STAGES_OPTIONS=[1]` for HIP | Yes — force `num_stages=1` |
| **Block ping-pong scheduler is MFMA-only** | RDNA4 (WMMA) gets none of the mfma/memory overlap | No — structural, would need new pass |
| **WMMA shape constraints (issue #8931)** | Non-16-multiple blocks fall to worse layout | Yes — keep BLOCK_M/N/K multiples of 16 |
| **`matrix_instr_nonkdim`, `kpack` are MFMA-only** | No-op on WMMA archs | N/A — strip from RDNA path |
| **IN_THREAD_TRANSPOSE only in ≥3.7** | +5.7-23.6% on bf16 FA missed if on 3.6.0 | Yes — upgrade Triton |

### 2.3 Recent merged RDNA4 perf wins (upstream Triton main)

| PR | Date | What |
|---|---|---|
| **#10185** | 2026-05-01 | IN_THREAD_TRANSPOSE default-on for gfx1201 (+5.7-23.6% bf16 FA) |
| **#10109** | 2026-04-23 | gfx12 cache modifiers for buffer/async ops |
| **#9662** | 2026-05-07 | Refine optimal swizzling for wavefront64 |
| **#9886** | 2026-05-07 | f32 WMMA in AccelerateMatmul |
| **#9825** | 2026-03-25 | Small M/N for scaled WMMA |
| **#8778** | 2025-11-20 | Enable buffer atomic for RDNA4 |
| **#8444** | 2025-10-15 | `v_dot` for bf16 multiplication on gfx11/gfx12 |
| **#7644** | 2025-08-01 | Scaled dot lowering on gfx12 |
| **#7374** | 2025-08-30 | Optimize chained multiplications for WMMA |
| **#7012** | 2025-06-17 | WMMA AMD16×16×32 support for i4 operands |

### 2.4 The big unlanded perf op

- **PR #7616** (draft since 2025-07-23): `[AMD] Enable global_load_tr_b128 on gfx12` — single-instruction 8×8 transpose-load that skips LDS for non-K-contig dot operands. **Exactly the operation attention K/V loads need.** Author stuck on layout propagation through pipelined ops. *"The biggest known unrealized RDNA4 perf win still gated."*

### 2.5 Other open Triton PRs to track

| PR | State | Why |
|---|---|---|
| **#9533** | Draft | `block_m=16` default for AMD — *validates our hardcoded BLOCK_M=16 choice* |
| **#9562** | Open | `maxnreg` for ROCm/AMD backend — VGPR allocation control, critical for RDNA4 occupancy |
| **#10254** | Open | LLVM flag passthrough for AMDGPU codegen — allows forcing scheduler/WMMA flags |
| **#9292** | Draft | Fixing WMMA.F32 conversion (gfx1250-focused but pattern applies) |
| **#9113** | Draft | Fine-grained `lgkmcnt` for compute-memory overlap (+40% BF16 GEMM on MI300X; transferable pattern) |
| **#9898** | Open | Reduce VGPR pressure for 4-stage chained dot schedule (MFMA-focused; chained-dot pattern applies to WMMA) |

### 2.6 ROCm/triton fork — RDNA-specific work not upstream

- `backport-rdna-matmul-config` head commit (2025-12-22): *"Improve matmul ogs config on RDNA"* — RDNA-specific matmul autotune configs.
- `rdna/3.3.x`: reverts upstream PR #6250 (DPP for RDNA) — fork found regression upstream didn't catch. **Footgun warning.**
- `shared/bypassLDS`, `enable-bypassLDS-by-default`: LDS-bypass perf path (load → register direct) enabled by default on fork only.
- `xinyazhang/navi-warpsize-32-oursolution`: wave32 default-warp-size handling for Navi 3x.
- `release/internal/3.5.x_llvm_scheduling_option`: fork-internal LLVM scheduling knob.
- `niz/ds_read_tr_fp4mn`: DS-read transpose specialized for FP4.
- `xinyazhang/aotriton-*`: **AOTriton — ahead-of-time Triton with prebuilt RDNA4 binaries.** No upstream equivalent. We do our own AOT compilation; worth comparing patterns.

### 2.7 PR #6250 footgun

Upstream PR #6250 (Mar 2025) added DPP instruction support for RDNA. The ROCm fork's `rdna/3.3.x` reverted it (commit `384edaf`, 2025-06-30) due to a regression upstream didn't catch. **If we upgrade Triton and hit RDNA-specific weirdness, this is a known land-mine.**

---

## Section 3 — vLLM (vllm-project/vllm)

### 3.1 The headline

**vLLM does not use AITER on RDNA4 at all.** All AITER paths gated by `on_mi3xx()`:

```python
# vllm/_aiter_ops.py:66-86
def is_aiter_found_and_supported() -> bool:
    if current_platform.is_rocm() and IS_AITER_FOUND:
        from vllm.platforms.rocm import on_mi3xx
        return on_mi3xx()   # gfx942/gfx950 ONLY
    return False
```

`VLLM_ROCM_USE_AITER=1` is silently ignored on RDNA4. Backend priority on gfx1201:
`[ROCM_ATTN (HIP custom paged), TRITON_ATTN, TURBOQUANT]`.

`vllm/platforms/rocm.py` logger line is explicit: *"RDNA3/RDNA4 (gfx11xx/gfx12xx): Use Flash Attention Triton backend."*

**This reflects pre-2026-05-19 AITER state.** vLLM's gate was correct when written; it's a timing artifact, not a permanent design verdict. When AITER's gfx1201 path matures, vLLM is likely to expand the gate.

### 3.2 Recently merged RDNA4 wins on vLLM main

| PR | Date | What | Win |
|---|---|---|---|
| #34709 | 2026-03-20 | wvSplitK skinny GEMM for RDNA4/gfx1x decode | ~15% decode tok/s on R9700 |
| #37826 | 2026-05-15 | Widen OAI Triton MoE capability to gfx12 | Enables `tl.dot_scaled` on gfx12 |
| #38086 | 2026-04-02 | Triton FP8 MoE for gfx1201 | +7-8% on Qwen3-30B-FP8 dual R9700 |
| #38455 | 2026-04-10 | RDNA 3.5/4 device IDs (gfx1150/gfx1151/gfx1201) | Platform recognition |
| **#40827** | 2026-04-24 | **LLMM1→vecMatMul + RDNA4 correctness fixes** | **Fixes silent-wrong-output bugs (`lane%32==0` hardcoded as half2 write guard on WARP_SIZE=32)** |
| #41187 | Open | LDS bank conflict fix in vecMatMul | RX 9070 XT |

PR #40827 is a cautionary tale: *"always pass on CDNA (WARP_SIZE=64) CI hardware"* — RDNA4 was producing silently wrong outputs in skinny GEMM until April 2026. CDNA-only CI doesn't catch wave32 bugs.

### 3.3 Open RDNA4 PRs / draft / important

- #34741 (open): FP8 KV-cache + RDNA4 custom PA. **Reports ~2.75× concurrency on R9700 via 132K→264K tokens.**
- #34740 (open): Migrate FP8 gates to `supports_fp8()` — unblocks RDNA4 FP8 paths.
- #36659 (draft): Enable FP8 inference on gfx1201 MoE.
- #38454 (open): RDNA4 backend selection tests.
- #38555 (open): Skip encoder cache profiling on consumer RDNA — workaround for MIOpen hang.
- #40254 (open): Enable all GPU arch in AITER docker build. *Reviewers skeptical — AITER's official targets exclude RDNA.*

### 3.4 ROCm/vllm fork

**RETIRED.** README is a one-line redirect: *"The ROCm/vllm repository is retired, please use the upstream repository."* All AMD work flows upstream. No secret RDNA4 work hidden in the fork.

### 3.5 Real-world RDNA4 numbers from community

- **ivangotoy/llama-server-rdna4-vulkan: llama.cpp Vulkan 62 t/s vs vLLM ROCm 48 t/s on RX 9070 XT.** llama.cpp is *already faster* than vLLM on this hardware.
- vLLM issue #28649: *"AITER's C++/ASM kernels do not work on RDNA4 and must be disabled (VLLM_ROCM_USE_AITER=0)... FP8 silently falls back to FP32 dequantization."* The author later updated saying tuned model JSON configs + arch-mapping patches at runtime are sufficient — basic FP8 path works. Reports Qwen3-0.6B 160→200 tok/s (+25%), Qwen3-30B 52→85 tok/s (+63%) on R9700, but attributes the win primarily to CUDA-graph improvements not FP8.

### 3.6 Active RDNA4 bugs

- #40980: TP=2 deadlock on dual R9700 (regression vs v0.14.0).
- #41472: ROCM_ATTN produces incorrect output for LiquidAI LFM2.
- #40081: vLLM fails to start on RDNA4 in unprivileged containers (amdsmi, circular import).
- #41964 (closed): EngineCore consumes 100% CPU at idle on Radeon AI Pro 9700.

### 3.7 RFC #40620 — strategic context

Documents the architectural debt of scattered `on_gfx*()` and `has_device_capability(N)`-style checks. Proposes semantic capability methods (`supports_fp8_native`, `supports_fp8_woq`, `get_architecture_family`). Open. Not tonight-actionable but signals that vLLM acknowledges the dispatch story is a mess for consumer SKUs.

---

## Section 4 — Cross-referencing with our existing context

### 4.1 Jira tickets directly impacted

| Ticket | Title | Status | This audit affects how |
|---|---|---|---|
| **MAD-196** | Wave32 vs wave64 unified_attention on RDNA4 | To Do | Original plan: "Triton AOT `--target hip:gfx1201:32` vs `:64`, expected 10-30% decode delta." Audit confirms upstream's host dispatcher uses wave32 by default on RDNA. Still worth measuring. |
| **MAD-198** | ~8% prefill regression at commit 27a255050 | To Do | Separate paged-tile issue, but our "vanilla_f16" baseline already includes this regression — actual gap is wider than reported. |
| **MAD-199** | AITER unified_attention TURBO KV support | To Do | Chunks A+B shipped on feat/aiter-turbo. The dispatcher/tuning work is a candidate for chunk D+E. |
| **MAD-189** | AITER MoE for batch=1 decode | To Do | Qwen3.6-A3B is MoE; AITER MoE compounds with attention wins. Defer until attention gap closed. |
| **MAD-192** | FP8/MXFP8 KV + AITER FP8 attention | To Do | vLLM #34741 (RDNA4 FP8 KV, ~2.75× concurrency) is the closest external prior art. |
| **MAD-65** | RDNA4 FP8 spike: AITER reuse vs native rocWMMA | To Do | AITER FP8 for gfx1201 just landed (PR #2621). Spike newly viable. |
| **MAD-186** (Epic) | AMD-focused llama.cpp fork productization | To Do | Audit informs the strategy of the epic. |

### 4.2 KG principles invoked

- `[[mad-lab-aiter-upstream-compat]]` — keep AITER integration upstream-compatible via `tl.constexpr` branching with marked local patches. **Any vendoring or dispatcher work needs to follow this.**
- `[[mad-lab-dev-test-both-paths]]` — bench AITER + paged-tile after every commit. **Any tuning sweep must include paged-tile regression check.**
- `[[turbo3-pivot-2026-05-19]]` — turbo3 is the production target; turbo4 retained for comparison.
- `[[wmma-tile-prefill-fixed-2026-05-20]]` — paged-tile WMMA decode bug fixed this morning; NIAH validates through 256K ctx.

### 4.3 Bench data (current state, post-D1+D2+D3)

```
                    8K prefill / decode    64K prefill / decode
vanilla_f16         2129 / 30              1548 / 51
paged_aiter_f16     1815 / 55              696  / 36
paged_aiter_turbo3  1172 / 53              305  / 31
paged_aiter_turbo4  1195 / 54              314  / 31
paged+turbo3        1791 / 33              949  / 32
paged+turbo4        1602 / 33              955  / 32
```

Observations:
- AITER+f16 already loses to vanilla_f16 (both prefill and decode). Not a turbo-specific issue.
- AITER+turbo3/4 prefill collapses at 64K (305-314) vs paged+turbo (949-955) — 3× regression.
- AITER+turbo decode at 8K is the only place AITER clearly wins (52-54 vs 33), but loses at 64K (31 vs 32).

---

## Section 5 — What the audit changes in our understanding

### 5.1 Things we believed that were wrong / incomplete

- **"AITER is structurally CDNA-only."** Wrong. Active RDNA4 enablement has been landing weekly for the past 8 weeks, with the foundational buffer-rsrc fix landing 3 days ago.
- **"vLLM uses AITER on RDNA4 and we should figure out how."** Wrong. vLLM gates AITER to MI3xx and explicitly tells RDNA4 users to use Triton FA. This is a timing artifact (vLLM hasn't caught up to AITER's last-week enablement), not a verdict.
- **"Our integration is just missing some constants."** Incomplete. We're missing the entire host-side dispatcher logic, AND we're vendoring a pre-bug-fix snapshot.
- **"gfx1250 is the next-gen RDNA4."** Wrong. gfx1250 = MI450 (CDNA). Only gfx1200/gfx1201 are RDNA4.

### 5.2 Things the audit confirmed

- Triton's RDNA4 codegen has real structural gaps (no block-pingpong, broken pipeliner for `num_stages>=2`, etc.) — confirmed by upstream tutorial code, multiple issues, and PRs.
- Our hardcoded `MT_AITER_UATTN_*` constants are CDNA-tuned. Upstream RDNA values differ on every knob except TILE_SIZE (`BLOCK_M=64 vs 16`, `cu_count*4` dispatch vs fixed 32 segments, etc.).
- llama.cpp's hand-written WMMA is competitive on RDNA4 (faster than vLLM ROCm on RX 9070 XT per community bench). Our paged-tile path is in the right ballpark for the hardware.

### 5.3 New things we now know

- **PR #3236 fixed a silent buffer-rsrc fallback bug** that our vendored kernel likely carries.
- **AITER added `arch_name`-aware tuning to the host dispatcher yesterday** (commit be0ca6a, gfx1151 + ≤2 KV heads) — gives us a clean pattern to mirror for gfx1201.
- **EmbeddedLLM/aiter fork is the active RDNA4 staging area.** Six branches; PRs #2351/#2352/#3168 still open.
- **The biggest latent perf op is Triton PR #7616** (`global_load_tr_b128`). Stuck in draft. Worth tracking; possibly worth contributing to.
- **AOTriton (ROCm/triton fork) ships prebuilt RDNA4 binaries.** No upstream equivalent. Pattern worth studying for our AOT.

---

## Section 6 — Options to discuss

### Option A — Re-vendor + minimal RDNA tuning (low risk, fast iteration)

1. Re-vendor `unified_attention.py` from AITER main HEAD → picks up PR #3236 (buffer-rsrc fix), PR #3228 (28 gfx1201 FP8 configs), be0ca6a (arch-aware tuning hook).
2. Confirm Triton version ≥3.7 (or pin to main) for IN_THREAD_TRANSPOSE.
3. Update wrapper `mt_aiter_uattn_shape_t` to pass `arch_name` through; let kernel dispatch on it constexpr-style.
4. Re-bench. **Hypothesis: substantial chunk of gap closes from PR #3236 alone.**

**Pros:** Smallest local diff. Stays close to upstream. Re-vendorable next time.
**Cons:** Doesn't fix the core dispatcher mismatch (we still have hardcoded `BLOCK_M=16`, `NUM_SEGMENTS_PER_SEQ=32`).

### Option B — Vendor the host dispatcher; full gfx1201 retune

1. Re-vendor as in A.
2. Vendor (or replicate in C++) the host dispatcher's gfx1201 logic: `TILE_SIZE`, `BLOCK_M`, `waves_per_eu`, `num_warps`, `num_stages=1`, `target_num_prgms = cu_count*4`.
3. Replace `MT_AITER_UATTN_*` hardcoded constants with arch-aware lookup.
4. Add a gfx1201 tuning branch to `unified_attention.py` mirroring the be0ca6a gfx1151 pattern, sweep `TILE_SIZE ∈ {16, 32, 64}`, `BLOCK_M ∈ {16, 32, 64, 128}`, `NUM_SEGMENTS ∈ {auto, 4, 8, 16, 32}` for our 16q/2kv head shape at 8K/32K/64K.

**Pros:** Aligns with upstream's emerging pattern. Could close the entire gap to vanilla_f16 (and beat it on long ctx).
**Cons:** Multi-day effort. Sweep cost ~20-50 runs. Risk of merging conflicts with upstream re-vendor in 2-4 weeks.

### Option C — Cherry-pick EmbeddedLLM/aiter fork content

1. Re-vendor as in A.
2. Cherry-pick the gfx1201 RDNA4 paths from EmbeddedLLM/aiter `support_gfx1201` branch: `v_pk_mul_f32` scalar fallback, DPP shuffle replacement, naive global→LDS for gfx12x.
3. Apply PRs #2351 (quant) and #2352 (rmsnorm) locally even though upstream hasn't merged them.

**Pros:** Gets us the broadest RDNA4 surface coverage (not just attention; also quant/rmsnorm/MoE).
**Cons:** We're now diverging from both AITER upstream AND EmbeddedLLM fork. Re-vendor cost grows.

### Option D — Acknowledge AITER on RDNA4 is research; lean into paged-tile as production

1. Keep AITER+turbo work for the eventual CDNA expansion (MI300/MI325/MI355 users).
2. Mark AITER+RDNA4 as experimental / research path.
3. Make paged-tile WMMA the production default on RDNA4. Fix MAD-198 (paged regression). Tune further.
4. Revisit AITER+RDNA4 in 1-2 months when upstream enablement matures.

**Pros:** Honest reflection of upstream state (RDNA4 enablement is one week old). Lower risk. Aligns with vLLM's current consensus.
**Cons:** Concedes the AITER work for now. AITER+turbo investment becomes harder to justify until CDNA expansion happens.

### Option E — Hybrid auto-dispatch

1. Detect arch at runtime: if CDNA → AITER default, if RDNA4 → paged-tile default.
2. Allow env override either direction.
3. Continue improving AITER+RDNA4 in parallel without it being the production path.

**Pros:** Matches the user's earlier framing ("non-AITER GPUs use the old path"). Both paths get exercised in different contexts.
**Cons:** Two production paths to maintain forever.

---

## Section 7 — Open questions for discussion

1. **Is our `feat/aiter-turbo` work intended to be production for RDNA4, or research that informs CDNA expansion?**
2. **Do we have time / appetite for a 2-3 day full retune sweep on R9700, or do we want a quick re-vendor + spot-check first?**
3. **Are we willing to diverge from AITER upstream by cherry-picking EmbeddedLLM/aiter fork content, accepting higher re-vendor cost?**
4. **How does this audit change MAD-186 (AMD-focused fork productization) strategy?** Does "AMD-focused" mean CDNA+RDNA, or do we have to pick?
5. **Is there value in contributing to Triton PR #7616 (`global_load_tr_b128`)?** Would be a 1-2 week investment but could unlock our biggest single latent perf win on RDNA4.

---

## Appendix — Key external references

### AITER
- Repo: https://github.com/ROCm/aiter
- May 2026 newsletter: `docs/newsletter/2026-05.md` (confirms gfx1250=MI450, gfx1201=Navi 48)
- PR #3236 (buffer-rsrc fix): https://github.com/ROCm/aiter/pull/3236
- PR #3228 (28 gfx1201 FP8 configs): https://github.com/ROCm/aiter/pull/3228
- commit be0ca6a (arch-aware tuning pattern): on main, 2026-05-20
- PR #2969 (FlyDSL gfx1201 FA): https://github.com/ROCm/aiter/pull/2969
- PR #2351 (quant for gfx1201): https://github.com/ROCm/aiter/pull/2351
- PR #2352 (rmsnorm for gfx1201): https://github.com/ROCm/aiter/pull/2352
- PR #3168/#3234 (gemm_a8w8 Qwen3/Mistral configs): https://github.com/ROCm/aiter/pull/3168
- EmbeddedLLM/aiter fork: https://github.com/EmbeddedLLM/aiter
- Issue #900 (gfx1201 support tracking): https://github.com/ROCm/aiter/issues/900
- Issue #1552 (supported arch list): https://github.com/ROCm/aiter/issues/1552

### Triton
- Repo: https://github.com/triton-lang/triton
- ROCm fork: https://github.com/ROCm/triton
- PR #7616 (global_load_tr_b128 — biggest latent win): https://github.com/triton-lang/triton/pull/7616
- PR #10185 (IN_THREAD_TRANSPOSE for gfx1201): https://github.com/triton-lang/triton/pull/10185
- PR #9533 (block_m=16 AMD default): https://github.com/triton-lang/triton/pull/9533
- PR #9562 (maxnreg AMD): https://github.com/triton-lang/triton/pull/9562
- Issue #8931 (WMMA layout small shape): https://github.com/triton-lang/triton/issues/8931
- ComfyUI-WanVideoWrapper #2007 (pipeliner UAF on gfx1201): https://github.com/kijai/ComfyUI-WanVideoWrapper/issues/2007
- Apollo-mg RDNA4 tuning guide: https://gist.github.com/apollo-mg/ecba6a0c29323325a7ac3babf08e53be

### vLLM
- Repo: https://github.com/vllm-project/vllm
- Retired ROCm fork: https://github.com/ROCm/vllm
- Issue #28649 (RDNA4 FP8 patch / AITER disable): https://github.com/vllm-project/vllm/issues/28649
- Issue #40081 (container startup): https://github.com/vllm-project/vllm/issues/40081
- Issue #40980 (TP=2 deadlock dual R9700): https://github.com/vllm-project/vllm/issues/40980
- PR #40827 (RDNA4 correctness fixes): https://github.com/vllm-project/vllm/pull/40827
- PR #34741 (FP8 KV-cache on R9700): https://github.com/vllm-project/vllm/pull/34741
- PR #34709 (wvSplitK skinny GEMM): https://github.com/vllm-project/vllm/pull/34709
- RFC #40620 (unified device capability): https://github.com/vllm-project/vllm/issues/40620
- ROCm vLLM blog (2026-02-27, CDNA-focused): https://blog.vllm.ai/2026/02/27/rocm-attention-backend.html
- Community bench (llama.cpp 62 vs vLLM 48 on RX 9070 XT): https://github.com/ivangotoy/llama-server-rdna4-vulkan
