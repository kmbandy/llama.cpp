2026-08-07 run ledger -- DS4-Flash cross-machine dispatch campaign, day 2.
Continues docs/dev/2026-08-06-all-runs.txt. Targets (kmbandy, set 08-06):
prefill 80-100 t/s, decode 10-20 (10 accepted without top-k/pruning).

MORNING SETUP: 2026 synced to dcae37a55; udev rule staged at
/var/tmp/99-rx480-no-runpm.rules on 2026 (AWAITING sudo install; runtime
power/control still "on", no reboot since 08-01). Boards claimed both boxes.

=====================================================================
CONF-LADDER DECIDER (07:37-08:25, 16 arms, interleaved 4 reps x
{0.99,0.9,0.6,0.4} x SPEC_NMAX=7, banked stack: tier 3G/3G, LEASE=256,
PREFETCH_HINT=1 SPEC_PAGEIN=1 HINTLOG=1; /var/tmp/conf-ladder.log on 2026)
=====================================================================
  arm     prefill decode  accept            arm     prefill decode
  c99r1    20.15   3.18   0.84286 (59/70)   c99r3    17.24   2.83  <- outlier
  c99r2    18.15   3.26   0.87324 (62/71)!  c99r4    17.43   3.19
  c90r1    19.47   3.24   0.75000 (72/96)   c90r3    19.55   2.62  <- outlier
  c90r2    17.22   3.27      "               c90r4    16.55   3.27
  c60r1    18.36   3.30   0.76744 (66/86)   c60r3    20.18   3.34
  c60r2    19.49   3.31      "               c60r4    18.14   3.35
  c40r1    19.59   2.93 <- outlier (0.70874, 73/103)
  c40r2    19.62   3.27   c40r3 18.26 3.26   c40r4   19.16   3.26

DECIDER VERDICT: SPEC_CONF=0.6 + SPEC_NMAX=7 IS THE OPERATING POINT.
  mean/spread: 0.6 -> 3.33 +-0.05 (3.30/3.31/3.34/3.35); 0.4 -> 3.18
  sp 0.34; 0.99 -> 3.12 sp 0.43; 0.9 -> 3.10 sp 0.65. Every 0.6 rep beat
  every other config's median. Knee confirmed at 0.6: 0.4 drafts more
  (103 attempts, accept 0.709) and DEMANDS more (verify 5061 req / 35366
  experts on R9700 vs 4397/31044 at 0.6) without paying.

THE 0.99-VARIANCE MYSTERY (last night's 0.40 spread): SOLVED-ENOUGH.
  Localization from per-phase per-worker spine stats (all arms):
  - Outlier arms lose time to EXTRA DISPATCH WAIT on byte-identical
    request streams (within-config trajectories replicate exactly;
    request counts per worker identical across reps).
  - The excess wait sits on the 2026 WORKERS, worst on the 480:
    c90r3 verify wait w480 48.6s vs twin 32.9s (+15.7), w1070 +14.0;
    c99r3 decode-phase w480 22.5s vs 17.2 (+5.3), w1070 +4.6; R9700
    FLAT in every outlier. c40r1 same shape.
  - c99r2 drew a DIFFERENT draft trajectory (62/71 vs 59/70) -- 0.99
    can flip trajectory run-to-run; 0.9/0.6/0.4 never did (temp-0
    determinism holds within them across 4 reps + last night).
  MECHANISM (consistent with all 16 arms): sparse/bursty drafting at
  high conf produces unpacked, irregular expert demand; the 2026 cards
  occasionally hit service-time races (eviction/spec-page-in timing)
  under that pattern. Dense packing at 0.6 (steady ~86-attempt cadence)
  smooths demand below the race threshold. The R9700 is innocent.
  ACTION: promote 0.6/nmax7 to config of record pending kmbandy sign-off;
  the 480-side race is the same contention family as prefill spec-LATE
  and gets attacked by the same worker-side gating work.

BUILD (mid-ladder, main): 2c02727b5 "expert-dispatch: cross-layer
predicted hints from the dispatch path" -- WP_PREDICT_AHEAD=k lookahead
hints (PREDICTED provenance, decode-gated, one router GEMM/layer) +
WP_PREDICT_CAPTURE (h_L + actual selection per layer per step; the
dispatch-path replacement for the destroyed WP_CAPTURE_ROUTING) +
docs/dev/analyze-pred-capture.py (k=1..4 precision decay = the day-one
falsifier). Compile-checked in build-cpu; build-hip NOT rebuilt until
the ladder finished (no mid-campaign binary swaps).

=====================================================================
CAPTURE + FALSIFIER (08:48-09:00)
=====================================================================
cap1 (08:48): harness on 2026 was STALE (pre-2c02727b5) -- PRED_CAPTURE
  silently ignored; run wasted for capture but banked 0.6 rep: 18.22/3.28.
  LESSON: harness edits land on MAIN but execute from 2026 -- sync FIRST.
  2026 ff-synced to 2c02727b5 (stash dance around foreign llama-context WIP).
cap2 (08:52, capture ON): 18.30/2.96, same 66/86 trajectory. Capture's
  synchronous fwrites cost ~0.35 decode -- CAPTURE ARMS ARE NOT BANKING
  REPS. 236 MB, 8825 records, 192 steps.

FALSIFIER VERDICT (analyze-pred-capture.py on cap2): THE JULY BASIS IS
DEAD. router_{L+k}(h_L) vs actual selection: k=1 R@1=15.7 R@3=42.0
R@6=66.0; k=2 58.7@6; k=3 51.5@6; k=4 48.7@6 (union-level, ~8 hinted
pages/layer). NOT 0.973 rank-1. Proof the measurement is sound: k=0
self-consistency 99.8%; RMSNorm gain-swap changes nothing; temporal
same-layer baseline 39.4% (matches known 0.33-0.42); cross-layer
expert-ID carryover 3.3% = chance (expert namespaces are per-layer).
Decay across k is GENTLE; the absolute level is ~6x below the basis.
Signal real (1.7x temporal) but weak -- the live A/B decides.

=====================================================================
SYNC PREDICTION A/B (09:00-09:15, PRED_AHEAD=2, SPEC_HOST=1 stack)
=====================================================================
  predbase   19.29/3.34    predbase2  20.14/3.34   <- +-0.00 replication
  predk2m3   18.95/3.00    predk2m3b  19.72/3.00   <- +-0.00 replication
  predk2m6   17.47/2.93
DECOMPOSITION (spine per-phase, OTHER = predictor GEMM on the dispatch
thread): base OTHER=0.2s vs pred OTHER=9.6-9.9s -- the GEMM alone is
the whole loss. BUT total worker wait DROPPED with hints on: verify
44.1->41.1s (-6.8%), every worker improved (r9700 43.2->40.8, w1070
41.5->38.7, w480 46.0->42.3). THE HINTS PAY; THE MATH PLACEMENT DOESN'T.
-> 294e05df4 "score predicted hints off-thread": latest-wins inbox +
scorer thread + flush at next layer's compute() entry (sockets quiet
there). One layer of lead spent on handoff; k=3 = effective 2-layer.
ASYNC A/B QUEUED: abase1/ak3m3/ak3m6/ak2m3/abase2/ak3m3b.

=====================================================================
ASYNC + SALVAGE SWEEP (09:16-10:13) -- the full prediction design space
=====================================================================
Async scorer (294e05df4): OTHER collapsed 9.6s -> 0.3s, but waits ROSE
(+10s verify in ak3m3): the sync GEMM had been an accidental throttle.
  abase1 3.35  ak3m3 2.95  ak3m6 3.17  ak2m3 2.64  abase2 3.34  ak3m3b 3.31
Low-volume high-precision arms (kmbandy's push; rank-1 precision measured
94.5% k=1 / 91.9% k=2 / 86.2% k=4 / 77.1% k=7 -- the July 0.973 basis
REPLICATES as per-rank precision; my "falsifier fired" was a metric
conflation with union-recall, corrected in KG 00ca796a):
  mk2m1 3.35  mk2m2 2.98  mbase1 3.35  mk2m1b 3.35  mk2m2b 3.29
Margin gate built (WP_PREDICT_CONF, 9d9b61f22; calibration k=7: 0.22 ->
88% @ 50% emission, 0.46 -> 92.5% @ 25%). k=7 M=1 sweep:
  sbase1 3.34  sk7c0 3.31  sk7c22 2.96*  sk7c46 2.64*  sbase2 2.98*
  sk7c22b 3.28  sk7c46b 3.32       (* = the 09:58-10:05 sag window)

CLOSURE -- THE COUNTERS THAT END IT (landed/promoted/wasted, all workers):
  sk7c0   1380 /  94 / 1286   incremental precision  6.8%
  sk7c22b  420 /  55 /  365                          13%
  sk7c46b  210 /  44 /  166                          21%
Offline precision rises with the gate exactly as calibrated; INCREMENTAL
precision tops out at 21% because router confidence and LRU residency are
the same signal (expert popularity). The pages prediction supplies cleanly
are the pages the cache already holds; the eviction tail that needs the
help is what the router cannot call confidently. Best arm: 44 useful
pages/run (~0.13s) vs similar wasted-read cost. 18 prediction arms, 6
baselines, every cell <= baseline. Converges with the GLM 2026-07-29
prefetch gate: PREFETCH IS A CONSEQUENCE OF POOL CAPACITY.
KEPT (inert by default): capture infra, per-rank calibration, async
scorer + conf gate, prefill spec gate (06bc72a45, UNTESTED).

OPEN: 2026-side service sag 09:58-10:05 (baseline 3.34->2.98, both 2026
workers +12s wait, r9700 +6; recovered by 10:08). NOT swap (none), NOT
runpm (still on), NOT NVMe temp (56C). MemAvailable 2.3 GB (below the
2.9 floor). iostat sampler staged: /var/tmp/io-sampler.sh on 2026 --
START IT BEFORE THE NEXT CHAIN.

=====================================================================
CODE-WORKLOAD PIVOT (11:41-12:25) -- kmbandy: "the goal is predominantly
coding and we're using prose to do all of this"
=====================================================================
New benchmark prompt code700.txt (47cabd046): real Python (sim-lease
Cache class excerpt), 493 tok, method-boundary cut, no template repeat.
  codebase1 14.84/3.74   codebase2 15.58/3.74  <- +-0.00, acc 0.91720
  codecap   12.29/3.74   (capture on; decode unaffected on code)
CODE IS DSPARK'S HOME TURF: acceptance 0.917 vs 0.767 prose, mean block
2.44 vs 1.77 -- decode 3.74 vs 3.33 at the SAME prose-tuned stack.

OFFLINE REANALYSIS ON THE CODE CAPTURE (114 steps): prediction closure
TRANSFERS. Per-rank precision k=2: r1 85% (vs 92 prose); first-non-
resident selector 28.3% @ depth p50=4 (vs 25.4/5); sim residency 74.6%.
Router-prefetch stays closed on the deployment workload.

DEPTH LADDER (conf 0.6): NMAX 10/14 byte-identical to 7 (144/157, 2.44)
-- the conf gate binds first; depth cap inert. 3.73/3.72/3.70/3.73.
CONF FOLLOW-DOWN (NMAX=7), all reps replicate +-0.03:
  conf 0.0 -> 3.29 (acc 0.514, 356 attempts -- verify traffic eats it)
  conf 0.1 -> 3.64 3.64      conf 0.2 -> 3.58 3.61
  conf 0.4 -> 3.83 3.83 <- THE CODE OPERATING POINT (acc 0.94194!)
  conf 0.6 -> 3.70-3.74
CODE CONFIG CANDIDATE: SPEC_NMAX=7 SPEC_CONF=0.4 + banked stack = 3.83
decode / ~15 prefill (493-tok prompt; fixed cost dominates).
NOTE the knee MOVED with workload (prose knee 0.6, code knee 0.4) --
acceptance-vs-conf is workload-dependent; retune per deployment corpus.

NEXT (proposed to kmbandy, pending go): turbo4 temp-0 byte gate (the
2026-08-03 rotation fix is IN today's binaries via the WIP tree) + timing
arm; then WP_DEFER_K 1/2/4 sweep on the code point -- prediction-free
read-latency hiding, built, never measured. 3.83 x 1.96 x ~1.3 ~= 10.

=====================================================================
THE PACKING THREAD (12:34-13:30) -- kmbandy: "the drive is idle 70% of
the time... these predictions need to be packing in"
=====================================================================
CLAW BANKING (code conf-0.4, k=2 M=1 no-preempt, 3x interleaved):
  clawb1 3.83  clawm1 3.82  clawb2 3.54*  clawm2 3.27*  clawb3 3.81
  clawm3 3.85 <- campaign-best on a HINT arm. (* = sag window)
  Counters: landed ~794, promoted 307-350 (39-44% incremental). BANKS.
LIVE DRIVE PROFILE (new /proc/diskstats sampler on 2026): decode util
10-24%, r_await 1.2-1.5 ms -- 76-90% IDLE, kmbandy confirmed live.
Prefill: 80-115% util, r_await 72-93 ms (deep queue). AND THE SAG HAS AN
ALIBI: during sag arms' decode the drive was NORMAL -- the 2026
degradation is NOT the drive (nor swap/runpm/temps). Next suspect:
kswapd reclaim storms (availMB ~2.3GB floor); sampler now logs
pgscan_kswapd + MemAvailable per 2s.
PREEMPTIBLE LANDINGS (31a22ebf1, WP_EXPERT_SPEC_PREEMPT): 1 MiB slices,
pause while demand serving. VOLUME RAMP (pkm*): collision cost GONE --
M=4 flat t/s (3.79/3.80 vs base 3.79/3.83) where M=3 used to cost 0.34.
BUT promotes COLLAPSED with volume: M=1 683/126, M=2 1560/184, M=4
4031/18(!). Mechanism: request-scope gate over-paused landings (~16
ms/page incl. compute phases when the drive is FREE); pages landed after
their layer passed. Deep-k as window fix REFUTED cleanly (dk*): k-axis
monotone AGAINST depth at fixed slow landings -- promotes 12% (k=2) ->
6.4% (k=4) -> 2.3% (k=7); staleness beats window. FRESHNESS + FAST
LANDINGS is the recipe.
-> 781550e9b: gate on OUTSTANDING DEMAND READS (demand_reads_pending_
in read_worker), not request scope. Landings pause only while the drive
is actually contended. TIGHT-GATE RERUN tg*: k=2 x M=1/2/4, prediction:
promotes recover past ~330 at M=1 AND scale with M.

TIGHT-GATE RERUN (13:29-13:46, 781550e9b, read-scoped preemption):
  tgbase 3.83  tgm1p 3.81  tgm2p 3.83  tgm4p 3.75  tgbase2 3.81  tgm4pb 3.75
  Fleet promotes: M=1 292, M=2 463, M=4 479. SCALING RESTORED (the
  coarse gate had M=4 at 18). Main's landing queue saturates at M=4
  (its promotes dip 263->143 while 2026's climb); M=4's -0.07 replicates.
PACKING VERDICT: PRED_AHEAD=2 PRED_TOPM=2 PREEMPT=1 IS THE OPTIMUM --
463 promotes/run (~6% of demand reads converted NVMe->tier) at EXACTLY
baseline t/s, tail benefits per the claw arms. BANKED into the code
config candidate: SPEC_NMAX=7 SPEC_CONF=0.4 + tier stack + k2m2+preempt.
STRUCTURAL CEILING REACHED for router-prefetch as a MEAN-t/s lever at
this pool size: conversion is bounded by landing throughput x the
residency-confidence correlation. It is a TAIL/robustness feature.
The mean-decode path forward is turbo4 (1.96x, byte gate pending) --
prediction now rides along for free.

=====================================================================
THE DECODE WALL, LOCATED (14:50-15:10) -- and the deferral burial
=====================================================================
kmbandy's catch: decode FROZEN at 3.75-3.85 across every prefetch
intervention -- the reads being optimized were not the marginal cost.
CORRECTION EN ROUTE: my "GPU compute is the wall" claim from cumulative
stat deltas was WRONG (overlapping buckets -- the 07-26 attribution
trap). The REQLOG per-request decomposition (svc1 arm, 5142 decode
requests, R9700) is the truth:
  request wall mean 7.96 ms (p50 6.9), n_exp 8.5, n_pagein 1.64:
    ns_read   4.55 ms (57%)  <- cold-read chain, request-scoped
    ns_h2d    1.66 ms (21%)  <- pageable staging, 14.1 GB/s
    ns_submit 0.96 ms (12%)
  self-bench: isolated 1-expert graph 87 us -- compute was never it.
Decode IS read-bound -- at REQUEST scope, not drive scope: the drive
idles 80% globally while every request waits ~4.5 ms serially for its
1-2 cold pages, 43 layers x ~8 ms = the 262 ms step.

DEFERRAL PROBE (dfbase/dfk4/dfk2, kmbandy predicted failure from GLM):
  base 3.73 / 0.94194   DEFER=4 3.47 / 0.84472   DEFER=2 3.70 / 0.92903
PREDICTION CONFIRMED AND WORSE: K=4 loses ten acceptance points AND
0.26 t/s. Mechanism: WP_DEFER_K defers by COUNT, not COLDNESS -- at 74%
residency it defers mostly resident (cheap) experts while cold reads
still block; all approximation cost, zero hiding. DS4 DEFERRAL =
SETTLED-NO. Do not re-raise.

THE RISK-FREE LADDER (kmbandy's frame: the R9700 is the only GPU on the
fast NVMe -- get the figure out): hardware floor per request ~2-2.5 ms
read+h2d (21 MB: ~2 ms parallel-striped at 6.2 GB/s proven + 0.75 ms
pinned at 28.3 GB/s PCIe4 x16 SAM) vs today's 6.2 ms.
  (1) WP_STAGING_PINNED=1 on the R9700 (pin.sh A/B RUNNING, metric =
      per-request ns_h2d 1.66 -> ~0.8)
  (2) parallel intra-page stripes in read_worker (~2-2.5 ms off ns_read)
  (3) SAM/ReBAR direct NVMe->VRAM -- the roadmap build; kills h2d
      entirely. Target: request ~4-4.5 ms => decode 5.5-6 byte-identical;
      turbo4's FIXED build gated on acceptance afterward.

=====================================================================
STRIPE-PARALLEL: TWO HANGS, TWO ROOT CAUSES, THEN THE FIRST REAL
DECODE WIN (15:35-16:36)
=====================================================================
sp1 hung 11 min (kmbandy caught it; watchdog monitors mandatory since).
750faf818 stripe-parallel v1: 1070 froze on first page-in batch.
d3d7c5630 fixed a REAL-BUT-SECONDARY shared_ptr race (lease copied
outside the writer's mutex). sp1r STILL hung (watchdog fired at 387s,
R9700 this time; 5 threads futex-parked, ptrace denied -- diagnosed by
thread census + arithmetic): *** c9c0c801b THE ACTUAL DEADLOCK:
PageShared pinned every page's staging lease for the BATCH lifetime;
prefill batches (~31 pageins) pin > the pool's 16 buffers; readers
exhaust the pool and sleep forever. Last-stripe COMPLETER now resets
the pin -- serial path's exact buffer lifetime. ***
VALIDATION (vbase/vs1, watchdog armed, clean window):
  vbase 14.21/3.76   vs1 16.62/3.89  acc BOTH 0.94194 (byte-identical)
  R9700 request: wall 8.07->7.38 ms (p50 6.78->5.71), read 4.55->4.09,
  wait 4.42->3.66. +3.5% decode, zero quality cost, no hang.
NEW CAMPAIGN BEST: 3.89 decode / 16.62 prefill (code700, banked stack +
conf 0.4 + k2m2 hints... note vs1 ran WITHOUT the pred hints -- the
stripe win stacks with the M=2 hint config, untested combined).
DAY TOTAL: 3.33 (prose record) -> 3.83 (code retune) -> 3.89 (stripe
parallel), all byte-identical-quality levers.

PUSH CHAIN (16:55-17:08, all on STRIPEPAR=1 base):
  combo1 3.80  combo2 3.58   (stripe + k2m2 hints: no gain over 3.89
                              stripe-alone; hints stay OPTIONAL tail insurance)
  chunk1 3.53* DIVERGENT TRAJECTORY (acc 0.85535, 136/159)
  chunk1b 2.73** canonical trajectory (deepest sag window of the day)
  *chunk1's divergence is NOT chunk numerics -- chunk1b at the same knob is
  canonical. It is the rig's residual nondeterminism (3rd sighting today;
  KG suspect: CPU DSpark worker threaded reduction order, 2026-08-03).
  **CHUNKS=1 unpriceable from this pair; park it for a quiet-window rep.
CONFIG OF RECORD (end of day): code700 + banked stack + SPEC_CONF=0.4 +
STRIPEPAR=1 = 3.89 decode / 16.6 prefill. THE SAG is now the dominant
measurement hazard (5+ windows today) -- top of tomorrow's list.
CONSULTS DISPATCHED: Codex gpt-5.6-sol (477e51fc/bc363725) + Kimi K3 via
pi (c182cbb2), full brief, Q1-Q5, analysis-only.

=====================================================================
THE SAG, RESOLVED-ENOUGH + CONSULT RETURNS (17:10-17:22)
=====================================================================
kmbandy's call: Brave/Chromium GPU acceleration on 2026. He killed
Brave; 4 config-of-record reps (nobr1-4): 3.86 / 3.91 / 3.93 / 3.90 --
ZERO sag windows, tightest+highest band of the day, prefill 14.3-16.6.
BRAVE CONVICTED (practical). Caveat: killing it also freed RAM, so
Kimi's direct-reclaim mechanism (pgscan_direct/PSI -- stalls readers
BEFORE the block layer, r_await stays clean, kswapd uncorrelated) is
not formally excluded; falsification arm = tier 3->2GiB on 2026.
*** END-OF-DAY CONFIG OF RECORD: code700 + banked stack + SPEC_CONF=0.4
+ STRIPEPAR=1, NO BROWSER ON 2026 = ~3.90 decode / ~16 prefill. ***
Day's decode line: 3.33 -> 3.83 -> 3.89 -> 3.90 band, all byte-identical.

CONSULTS (full texts in handoff channel, tasks 477e51fc + c182cbb2):
Codex gpt-5.6-sol + Kimi K3 converge: async H2D on the compute stream
(MAD-114-safe), coalesced small uploads, shape-keyed graph cache with
the HIP-graph key fixed, stripes 6-8 + thread cap raise. Joint est:
request 7.38 -> 4.5-5.0ms => decode 5.2-5.8.
NEW DEFECTS (Kimi): D1 ~17 sync device-wide tensor_set calls/request
for ~68-byte routing/idx uploads (0.35-1.0ms, THE 1.34ms accounting
hole); D2 WP_HIP_GRAPHS can never warm up on the worker (graph key =
node ptr in a per-request ggml context). D3 (both): the f16
request-direction worry is STALE -- f32 since PIPE_VERSION 4. CLOSED.
UNIQUE LEVERS: WireGuard ~0.6ms x 43 layers ~= 26ms/token when 2026
straggles -> plain-TCP A/B (+<=8%); draft/verify pipelining (pre-draft
block n+1 during verify n, output-exact, potentially +20-30% -- measure
draft share first); WP_KEEPALIVE_US never armed on R9700 (free arm);
prefill: offset-sort batch page-ins (~30 lines, byte-identical) +
UN-GATE predicted hints at prefill width (pure config -- dense demand
inverts the recall regime that killed decode prefetch).
PREFILL TARGET CORRECTED (Codex, verified from vs1 log): 67.55 GiB of
prefill page-ins / 493 tok -> HARD 42 t/s ceiling on the R9700 leg at
6.2 GB/s. My ~170 ceiling was 4x wrong. 80-100 prefill NEEDS hardware
(faster/more storage or +35-45GiB host RAM); software ceiling ~25-40.

=== ROUND 3 (evening): D1 coalesce + env arms + code2000 prefill baseline ===
Chain /var/tmp/round3.sh on 2026, all arms config of record + noted deltas.
Commits: 295d84fad (WP_EXPERT_PARAMS_COALESCE + WP_EXPERT_READ_WORKERS),
8b1ad64b9 (code2000.txt), c8b378a9a (KEEPALIVE_MAIN knob).

arm    decode  prefill  acceptance          trajectory
base   3.57    15.18    0.87821 137/156     DIVERGED (first-arm pattern)
d1     3.86    13.87    0.87952 146/166     diverged
d1gm2  3.94    14.83    0.94194 146/155     CANONICAL
rw8s6  3.88    16.02    0.92453 147/159     near-canonical flip
keep   3.94    15.95    0.94194 146/155     CANONICAL
code2k 3.39    26.15    0.86047 148/172     (2084-tok prompt, UBATCH=2048)

VERDICTS:
- COALESCE (D1): ADOPT. Both canonical arms hit 3.94 vs the 3.86-3.93
  pre-D1 band. Trajectory-matched REQLOG (vs nobr2/3): pg0 wall 2.40/2.94
  -> 1.70 ms (-0.7 to -1.2); pg+ ~neutral (keep 9.04 vs 9.10 baseline;
  d1gm2's 9.74 was arm noise). ns_params_set: 0.049 ms pg0 / 0.198 pg+
  (multiple compute waves x chunks). Kimi's 0.35-1.0 ms estimate for the
  old 17 syncs does NOT replicate as removable time -- the win is ~0.2-0.3
  ms/req net, concentrated in pagein-free requests. BYTE-IDENTITY CONFIRMED
  (canonical trajectory reproduced exactly under COALESCE, twice).
- GM2 (GATHER_MIN_TOKENS=2): NEUTRAL (d1gm2 3.94 = keep 3.94 without it).
- READ_WORKERS=8 + READ_STRIPES=6: NO GAIN. ns_read flat 5.72->5.77 (pg+
  cohort). The read path is at its floor; stripe lever CLOSED at decode.
- KEEPALIVE_MAIN=100: NEUTRAL on the R9700 (3.94 = 3.94). CLOSED.
- code2k PREFILL: 26.15 t/s at 2084 tok (4.2x tokens -> 1.72x t/s). The
  2084=2048+36 split means the 36-token remainder sweep pays near-full
  sweep bytes -- ~40 s/sweep vs 32.5 at 493 tok. Scaling with prompt
  length CONFIRMED directionally but we are at ~19% of the ~135 ceiling
  at this width (vs ~37% at 493) -- prefill levers (#10) have MORE
  headroom at deployment widths, not less. Decode-after-long-prefill 3.39
  on a divergent trajectory (deeper KV + nondet).
- NONDETERMINISM WORSE TODAY: base diverged hard (137/156, acc 0.878,
  mean 2.29 -- cost ~0.3 t/s on its arm). First-arm divergence pattern now
  seen twice (nobr1 yesterday, base today) -- suspect cold-start state on
  the CPU DSpark worker. Canonical-trajectory arms remain exactly
  reproducible, INCLUDING under COALESCE.
NEW CONFIG OF RECORD: + COALESCE=1 (decode ~3.94).
